services:
  postgres:
    image: postgres:14.6
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - ./storage:/var/lib/postgresql/data
    networks:
      - dev-net

networks:
  dev-net:
    external: true
